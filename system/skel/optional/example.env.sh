#!/bin/bash

# set custom apt packages that should be installed on codec startup
export CODEC_APT_PACKAGES="build-essential"

# set custom npm packages that should be installed on codec startup
export CODEC_NPM_PACKAGES="typescript is-up-cli npm-name-cli"
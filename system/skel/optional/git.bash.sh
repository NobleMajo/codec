#!/bin/bash

export LESS="-R"

git config --global pull.rebase true
git config --global rebase.autoStash true

git config --global user.email "majo@coreunit.net"
git config --global user.email "majo"
 